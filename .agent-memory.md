# Agent Memory - Persistent Knowledge Base

**Last Updated**: 2026-03-17 07:30 UTC

## Important Facts Learned

### Docker Environment
- Running inside Docker container named `ordinal-agent`
- Container IP: `172.27.0.x` (typically `172.27.0.2`)
- Container hostname: `ordinal-agent`
- VPS external IP: **Get dynamically with `./.get-public-ip.sh` or `curl -s ifconfig.me`**
- Workspace: `/home/claude/workspace`
- User: `claude` with sudo access
- Privileged container with Docker-in-Docker (DinD) enabled

### Port Mappings (CRITICAL - READ THIS FIRST!)
- **8000-8020**: Custom application ports - **ALWAYS USE THESE** for Express, Flask, custom servers
- **5000-5020**: Agent/subagent CC-Web ports - **RESERVED, DO NOT USE** for custom apps
  - Main agent: port 5000 (AGENT_BASE_PORT, but actually uses 5000 in .env)
  - Subagent N: port 5000 + N (5001-5020)
- **5080**: Open WebUI (chat interface)
- **4000**: cc-bridge (internal only)
- Ports MUST be mapped in `docker-compose.yml` to be accessible externally
- Always test external access at: `http://$(curl -s ifconfig.me):<port>`

### Port Testing History
- Attempted port 3000: ❌ Not mapped, refused connection
- Attempted port 8080: ❌ Not mapped, refused connection
- Used port 5001: ✅ Worked (agent port, but wrong for custom apps)
- **Current**: port 8000 ✅ **CORRECT** port for custom apps

### Active Services
- **Express test server**: Running on port 8000
  - File: `/home/claude/workspace/server.js`
  - Routes: `/`, `/api/status`, `/api/echo` (POST)
  - External access: `http://$(curl -s ifconfig.me):8000`
  - Started with: `node server.js > /tmp/express.log 2>&1 &`
  - PID: Check with `ps aux | grep "node server"`

### Project: Ordinal Agents (Subagent Orchestration System)
- **Purpose**: Single powerful AI agent with ability to spawn 1-20 specialized subagents
- **Git Repository**: https://github.com/miguelangelo78/ordinal-agents
- **Architecture**:
  - God container (this one) with Docker-in-Docker
  - Spawns subagent containers using `./agents.sh`
  - Each subagent gets isolated workspace + CC-Web instance
  - Max 20 subagents (IDs 1-20)

### Key Files & Directories
- `CLAUDE.md` - Instructions for agent (checked into git)
- `.agent-memory.md` - THIS FILE - persistent knowledge across redeploys
- `.get-public-ip.sh` - Script to fetch VPS public IP dynamically
- `agents.sh` - Subagent management script (spawn/stop/rm/list/logs)
- `docker-compose.yml` - Defines services, port mappings, volumes
- `.env.example` - Template for environment configuration
- `server.js` - Express test server on port 8000
- `subagents/` - Subagent configurations and CLAUDE.md files
- `subagents/template/` - Base template for spawning subagents

### Subagent Management Commands
```bash
./agents.sh spawn <1-20> [--role "description"]  # Create subagent
./agents.sh stop <id>                             # Stop subagent
./agents.sh rm <id>                               # Remove subagent + data
./agents.sh list                                  # List all subagents
./agents.sh logs <id>                             # View subagent logs
```

### Git Workflow
- Branch: `main`
- Status: Currently 2 commits ahead of origin
- Recent commits:
  1. Add persistent memory system (this file)
  2. Add port configuration docs + Express server
  3. Add redeploy script
  4. Update port ranges to 8000-8020
- Uncommitted: hello_world.py, node_modules, package files

### Package Management
- Node.js installed with npm
- Express v5.2.1 installed
- package.json exists with workspace metadata

## Common Mistakes to Avoid
1. ❌ Don't use ports below 8000 for custom apps
2. ❌ Don't assume ports are accessible without checking docker-compose.yml
3. ❌ Don't forget you're inside a Docker container (not on the host)
4. ❌ Don't use 5000-5020 range for custom applications
5. ❌ Don't forget to read this file after redeployment!
6. ✅ **ALWAYS use ports 8000-8020 for custom applications**
7. ✅ Always verify external access with VPS public IP (fetch with `./.get-public-ip.sh`)
8. ✅ Read CLAUDE.md first, then read THIS file immediately after
9. ✅ Update this file when learning new facts
10. ✅ Never hardcode public IP in committed files - use dynamic fetch

## User Preferences & Behavior
- User redeploys agent frequently - expects persistent memory
- User wants concise, direct responses with code (not explanations)
- User expects me to remember Docker context and port rules
- User wants to see code implementations, not descriptions
- User values taking ownership and verifying things work
